/*! ========================================================================
 * Appended after third-party JavaScript. */

$(function ()
{
    'use strict';
    autosize($('textarea:not(.code)'));
});
